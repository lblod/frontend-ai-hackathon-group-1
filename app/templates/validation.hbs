{{page-title "Validations"}}

<h1 class="page-title">Validate the AI output by comparing it to the source</h1>

<div class="validation-container">

  <table class="validation-table validation-table-bordered">
    <thead>
      <tr>
        <th>Source</th>
        <th>AI output</th>
        <th>Feedback</th>
      </tr>
    </thead>
    <tbody>
      {{#each this.model as |validation|}}
        <tr>
          <td>
            <a
              href="{{validation.annotation.resource}}"
              target="_blank"
              rel="noopener noreferrer"
            >
              {{validation.annotation.resource}}
            </a></td>
          <td>{{validation.annotation.body}}</td>
          <td>
            <button class="thumbs-up" type="button">👍</button>
            <button class="thumbs-down" type="button">👎</button></td>
        </tr>
      {{/each}}
    </tbody>
  </table>

</div>